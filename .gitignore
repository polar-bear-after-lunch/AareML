# Data files (too large for git — use download_data.py to fetch)
data/camels_ch_chem/
data/camels_ch/
data/usgs/
data/*.zip
data/*.parquet

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
*.egg-info/
.env
venv/
.venv/

# OS
.DS_Store
Thumbs.db

# Results (optional — uncomment to track)
# results/

# Private/personal files — never commit
outreach_emails.md
researcher_bios.md
researchers_to_contact.md

# Optuna study database (auto-regenerated, can get large)
results/optuna_study.db

# Large model checkpoints (auto-generated by notebooks)
results/*.pt


# ── Data directories — use download_data.py to fetch ──────────────────────
# Never commit raw data to git — use download_data.py instead
data/camels-ch-chem/
data/camels-ch-base/
data/lakebed-us/
data/swiss-lakes/
# Keep the data directory itself (for .gitkeep)
!data/.gitkeep

# Build scripts — kept locally, not in repo
build_effort_log.py
build_report.py
create_notebook_06.py
factcheck_report.md
fetch_from_ubelix.sh
sync_to_ubelix.sh
sandbox_brainstorm.md
AareML_InnovationSandbox_Application.docx
AareML-results-deepdive.pdf

# Russian versions — available on request
AareML-plain-language-ru.pdf
AareML-report-ru.pdf

# Old presentation version
AareML-presentation-new.pptx
