# AareML — Python dependencies
# Pinned to versions confirmed working on Python 3.11 (April 2026)
# Install: pip install -r requirements.txt
# PyTorch CPU: pip install torch --index-url https://download.pytorch.org/whl/cpu

# Core
numpy==1.26.4
pyarrow==16.1.0             # required for reading LakeBeD-US parquet files
pandas==2.2.2
matplotlib==3.9.2
seaborn==0.13.2
scipy==1.13.1
statsmodels==0.14.6
scikit-learn==1.5.2

# Deep learning
torch==2.3.1                   # CPU build fine for dev; install GPU build for training

# Hyper-parameter search
optuna==3.6.1

# Attribution / interpretability
captum==0.7.0                  # GradientSHAP on LSTM
shap==0.46.0                   # TreeSHAP on GBM surrogate

# GBM for catchment-attribute surrogate (notebook 05)
lightgbm==4.5.0                # preferred
# xgboost==2.0.3               # alternative

# Geospatial (optional — for station map; falls back to scatter)
geopandas==0.14.4

# Notebook tooling
jupyter==1.1.1
nbconvert==7.16.4

# llvmlite / numba (required by shap — install via conda to avoid cmake build)
# conda install -c conda-forge llvmlite numba
# numba==0.60.0
# llvmlite==0.43.0
